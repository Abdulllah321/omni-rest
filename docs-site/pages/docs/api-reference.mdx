import { Tabs, Callout, Cards, Card } from 'nextra/components'
import { Server, Type, List, Database, Layers } from 'lucide-react'

# API Reference

Complete reference for all Omni Rest endpoints.

## Overview

<Callout type="info" emoji="💡">
  Each Prisma model automatically gets 8 REST endpoints generated instantly without configuration.
</Callout>

<Cards>
  <Card icon={<List />} title="LIST" href="#list---get-all-records" />
  <Card icon={<Type />} title="CREATE" href="#create---create-new-record" />
  <Card icon={<Database />} title="READ" href="#read---get-single-record" />
  <Card icon={<Layers />} title="UPDATE" href="#update---replace-record" />
  <Card icon={<Server />} title="PATCH" href="#patch---update-record" />
  <Card icon={<Type />} title="DELETE" href="#delete---delete-record" />
  <Card icon={<Layers />} title="BULK_UPD" href="#bulk-update---update-multiple-records" />
  <Card icon={<List />} title="BULK_DEL" href="#bulk-delete---delete-multiple-records" />
</Cards>

## Query Parameters

Omni Rest supports powerful filtering, sorting, and pagination via standard query parameters.

<Tabs items={['Pagination', 'Sorting', 'Filtering']}>
  <Tabs.Tab>
    ```javascript copy
    GET /api/product?limit=10&offset=20
    ```

    | Parameter | Type | Default | Description |
    |-----------|------|---------|-------------|
    | `limit` | number | 10 | Maximum number of records to return |
    | `offset` | number | 0 | Number of records to skip |
  </Tabs.Tab>
  <Tabs.Tab>
    ```javascript copy
    GET /api/product?orderBy=price:desc&orderBy=name:asc
    ```

    - Use `field:asc` for ascending order
    - Use `field:desc` for descending order
    - Multiple `orderBy` parameters are supported
  </Tabs.Tab>
  <Tabs.Tab>
    ```javascript copy
    GET /api/product?name=Laptop&price[gte]=100&price[lte]=1000&inventory[gt]=0
    ```

    #### Filter Operators

    | Operator | Description | Example |
    |----------|-------------|---------|
    | `eq` | Equal | `name=MacBook` |
    | `ne` | Not equal | `name[ne]=MacBook` |
    | `lt` | Less than | `price[lt]=1000` |
    | `lte` | Less than or equal | `price[lte]=1000` |
    | `gt` | Greater than | `price[gt]=100` |
    | `gte` | Greater than or equal | `price[gte]=100` |
    | `in` | In array | `categoryId[in]=1,2,3` |
    | `nin` | Not in array | `categoryId[nin]=1,2,3` |
    | `contains` | Contains substring | `name[contains]=Mac` |
    | `startsWith` | Starts with | `name[startsWith]=Mac` |
    | `endsWith` | Ends with | `name[endsWith]=Book` |
  </Tabs.Tab>
</Tabs>

## Endpoints

### LIST - Get All Records

<Tabs items={['Request', 'Response', 'Status Codes']}>
  <Tabs.Tab>
    **GET** `/api/:model`

    Returns a paginated list of records.

    **Query Parameters:**
    - `limit` (optional): Number of records per page
    - `offset` (optional): Number of records to skip
    - `orderBy` (optional): Sort field and direction
    - Filter parameters (optional): See filtering section
  </Tabs.Tab>
  <Tabs.Tab>
    ```json copy
    {
      "data": [
        {
          "id": 1,
          "name": "Laptop",
          "price": 999.99,
          "createdAt": "2024-01-01T00:00:00.000Z"
        }
      ],
      "total": 1,
      "limit": 10,
      "offset": 0
    }
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    - `200` - Success
    - `400` - Bad request (invalid query parameters)
  </Tabs.Tab>
</Tabs>

### CREATE - Create New Record

<Tabs items={['Request', 'Response', 'Status Codes']}>
  <Tabs.Tab>
    **POST** `/api/:model`

    Creates a new record. Provide a JSON object with model fields (excluding auto-generated fields like `id`, `createdAt`).

    ```json copy
    {
      "name": "New Product",
      "price": 29.99,
      "inventory": 100
    }
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```json copy
    {
      "id": 2,
      "name": "New Product",
      "price": 29.99,
      "inventory": 100,
      "createdAt": "2024-01-01T00:00:00.000Z",
      "updatedAt": "2024-01-01T00:00:00.000Z"
    }
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    - `201` - Created
    - `400` - Bad request (validation error)
    - `409` - Conflict (unique constraint violation)
  </Tabs.Tab>
</Tabs>

### READ - Get Single Record

<Tabs items={['Request', 'Response', 'Status Codes']}>
  <Tabs.Tab>
    **GET** `/api/:model/:id`

    Returns a single record by ID.

    **Path Parameters:**
    - `id` - Record ID
  </Tabs.Tab>
  <Tabs.Tab>
    ```json copy
    {
      "id": 1,
      "name": "Laptop",
      "price": 999.99,
      "createdAt": "2024-01-01T00:00:00.000Z",
      "updatedAt": "2024-01-01T00:00:00.000Z"
    }
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    - `200` - Success
    - `404` - Not found
  </Tabs.Tab>
</Tabs>

### UPDATE - Replace Record

<Tabs items={['Request', 'Response', 'Status Codes']}>
  <Tabs.Tab>
    **PUT** `/api/:model/:id`

    Replaces an entire record.

    **Path Parameters:**
    - `id` - Record ID

    **Request Body:**
    Complete JSON object with all required fields.
  </Tabs.Tab>
  <Tabs.Tab>
    Updated record object.
  </Tabs.Tab>
  <Tabs.Tab>
    - `200` - Success
    - `400` - Bad request
    - `404` - Not found
    - `409` - Conflict
  </Tabs.Tab>
</Tabs>

### PATCH - Update Record

<Tabs items={['Request', 'Response', 'Status Codes']}>
  <Tabs.Tab>
    **PATCH** `/api/:model/:id`

    Partially updates a record.

    **Path Parameters:**
    - `id` - Record ID

    **Request Body:**
    JSON object with only the fields to update.

    ```json copy
    {
      "price": 899.99,
      "inventory": 50
    }
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    Updated record object.
  </Tabs.Tab>
  <Tabs.Tab>
    - `200` - Success
    - `400` - Bad request
    - `404` - Not found
  </Tabs.Tab>
</Tabs>

### DELETE - Delete Record

<Tabs items={['Request', 'Response', 'Status Codes']}>
  <Tabs.Tab>
    **DELETE** `/api/:model/:id`

    Deletes a record by ID.

    **Path Parameters:**
    - `id` - Record ID
  </Tabs.Tab>
  <Tabs.Tab>
    ```json copy
    {
      "message": "Record deleted successfully"
    }
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    - `200` - Success
    - `404` - Not found
  </Tabs.Tab>
</Tabs>

### BULK UPDATE - Update Multiple Records

<Tabs items={['Request', 'Response', 'Status Codes']}>
  <Tabs.Tab>
    **PATCH** `/api/:model/bulk/update`

    Updates multiple records in one request.

    ```json copy
    {
      "ids": [1, 2, 3],
      "data": {
        "status": "active",
        "updatedAt": "2024-01-01T00:00:00.000Z"
      }
    }
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```json copy
    {
      "updated": 3,
      "records": [
        { "id": 1, "status": "active" },
        { "id": 2, "status": "active" },
        { "id": 3, "status": "active" }
      ]
    }
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    - `200` - Success
    - `400` - Bad request
    - `404` - Some records not found
  </Tabs.Tab>
</Tabs>

### BULK DELETE - Delete Multiple Records

<Tabs items={['Request', 'Response', 'Status Codes']}>
  <Tabs.Tab>
    **DELETE** `/api/:model/bulk/delete`

    Deletes multiple records in one request.

    ```json copy
    {
      "ids": [1, 2, 3]
    }
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```json copy
    {
      "deleted": 3
    }
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    - `200` - Success
    - `400` - Bad request
    - `404` - Some records not found
  </Tabs.Tab>
</Tabs>

## Error Responses

All endpoints return consistent error responses:

```json copy
{
  "error": {
    "message": "Record not found",
    "code": "NOT_FOUND",
    "details": {}
  }
}
```

## Authentication

If your application requires authentication, implement it as middleware before mounting the omni-rest adapter:

```typescript copy
app.use("/api", authenticateMiddleware, expressAdapter(prisma));
```